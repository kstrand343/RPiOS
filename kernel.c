#include <stdint.h>
#include "mem_access.h"

int main()
{
  while(1)
  {
    // Turn on LED_ACT
    // Turn off LED_PWR
    // Wait 2 seconds
    // Turn off LED_ACT
    // Turn on LED_PWR
    // Wait 1 second
  }
  return -1
}
